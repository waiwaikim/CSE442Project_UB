<?php include_once('submissionSQL.php'); ?>

<?php
  $email = $_COOKIE["email"];
  $team_members = getTeamates($email);
  foreach($team_members as $name) {
    if (isset($_POST[''])) {
      
    }
  }     
?>
